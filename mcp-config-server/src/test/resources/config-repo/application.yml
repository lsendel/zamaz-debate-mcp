# Test configuration repository - Global defaults
spring:
  datasource:
    driver-class-name: org.h2.Driver
    
management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.zamaz.mcp: INFO